🗓️ Leap Year Calculator — ASP.NET Core MVC
A beginner-friendly Leap Year Calculator built with ASP.NET Core MVC, showcasing clean validation, controller logic, and Razor UI. Designed as a portfolio-ready project with future full-stack expansion using Vue.js.
🚀 Features
- ✅ Input validation using [Required] and [Range] attributes
- ✅ Razor form with Bootstrap styling
- ✅ Leap year logic based on Gregorian calendar rules
- ✅ Timestamped history entries (optional enhancement)
- ✅ Clear error messages via ValidationSummary and asp-validation-for
- ✅ Launches directly to /Home/LeapYear via launchSettings.json







🧠 How It Works
- User enters a year
- Form posts to HomeController.LeapYear
- Validation ensures input is numeric and within range
- Controller checks if the year is a leap year
- Result is displayed with optional history log


🌱 Next Steps
This project will evolve into a full-stack app:
- 🔄 Convert controller logic into a Web API
- 🎨 Build a Vue.js frontend to consume the API
- 🧪 Add unit tests for leap year logic
- 🗂️ Store history in session or database
- 📱 Make it mobile-friendly

